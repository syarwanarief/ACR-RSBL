<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.project.rsbl">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Cek Kamar"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="mobile.project.screenactivity.HomeActivity" />
        <activity android:name=".ScreenActivity.HomeActivity" />
        <activity android:name="mobile.project.MenuUtama.HomeActivity" />
        <activity android:name=".AbdulMoeloek" />
        <activity android:name=".RSAdvent" />
        <activity android:name=".RSimanuel" />
        <activity android:name=".RSuripSumoharja" />
        <activity android:name=".RestuBunda" />
        <activity android:name=".RS_dadi" />
        <activity android:name="mobile.project.ViewRoom.ViewAdvent"></activity>
    </application>

</manifest>